<UserControl x:Class="VSIXProject1.ToolWindow1Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300"
             Name="Baseline" Width="736.603" >
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Margin="10,10,10,10" HorizontalAlignment="Left" FontSize="20"><Run Text="Baselining"/></TextBlock>
            <DataGrid Name="workItems" AutoGenerateColumns="True" ItemsSource="{Binding}" HorizontalAlignment="Stretch" Height="245" VerticalScrollBarVisibility="Auto" Margin="0,0,-0.333,0">
                </DataGrid>
            
        </StackPanel>
    </Grid>
</UserControl>
